# PVS Handover — 2026-09-09 (updated 15:25)

In-flight session state for the next operator (e.g. a Fable session). Durable facts live in the
`pvs-*` memory files; this file holds the **moving** state — deployment matrix + open threads.
Git: branch `session-2026-09-03`, HEAD `975f5eb`+ (nas-daiya/docker-compose.yml has an uncommitted change from the MCS/robot session — not mine).

### Delivery robot (Reeman) — MCS dispatcher LIVE on the NAS 2026-09-07 16:46; PVS side BUILT, NOT deployed (additive-only; Danial: "don't touch anything else in PVS")
- **Robot = Reeman SLAM AMR** (flat open plate + pole display; API doc `Downloads/robot/REEMAN SLAM WEB API 3.0-*.pdf`). Danial has taught one delivery point per line + a store standby point on the robot.
- **Single writer to the robot = the MCS app dispatcher on the NAS** (`Documents/Dantec/MCS/app/Robot/*`, `/api/robot/*`, launcher tile → `robot.html`; wait times live-editable there). PVS only asks it.
- **PVS changes (uncommitted, in `src/`):** `LineConfig.Robot.DispatcherUrl` (new section, optional); `Runtime/RobotCaller.cs`; endpoints `POST /api/robot/call`, `GET /api/robot/status`, `POST /api/robot/done`; verify.html: 🤖 Call robot button + status chip (tap chip when AT LINE = release). No DB writes, no other PVS behaviour touched. Rule tree: `docs/pvs-rule-trees.md` → "Call delivery robot". 440 tests pass.
- **Deploy configs** `deploy/line1-5.config.json` gained `"robot": { "dispatcherUrl": "http://192.168.0.169:8090" }` — goes out with the next rollout (canary rule still applies; a live config-edit + restart also works once the build is on a box).
- **NAS is live:** robot `192.168.0.115` (RSN v3.2.0, JSON nav_status), points `line-1..5` / `StandByPoint` / `Charging-Pile`; `gmssmt1:8090/robot.html` works; first real trip to line-1 and back done 16:41–16:44 from the laptop dispatcher (stopped after — ONLY the NAS dispatcher may run against the robot now; never run `mcs-web` locally with `ROBOT_HOST` set while the NAS one is up, two dispatchers will fight over it).
- **L1 DEPLOYED 2026-09-07 16:57** (robot build + live config edit, see matrix). L2–L5 still without the button: they get it with their next rollout + the `robot` config line (already in `deploy/line2-5.config.json`, but the box configs need the same live edit — the deploy script never copies config). First L1 probe said "dispatcher not reachable" for ~20 s after restart (HttpClient warm-up) — transient, not a network problem.

### Parts requests (line -> store terminals -> robot) — BUILT + MCS LIVE 2026-09-07 18:42; PVS build `df3a1b2` on ALL FIVE LINES (L2-L5 via the idle watcher, done by 19:46)
- **Flow:** PVS `PartsRequestService` reads its own `/api/exhaust` every 60 s; when a feeder `needsRequest` (won't last the lot, no spare staged, lot under-issued) is within `robot.requestMinutes` (45) of run-out it files ONE request per part on MCS `POST /api/requests`. Store Terminals 1+2 (sitting on `go.html`) auto-open `requests.html`; the store keeper acknowledges, scans reels onto the request (**= MCS StockOut INSERT**, `ReqNumber=REQ-<id>`), taps Send (robot dispatched, reason `REQ-<id>`) or **Dismiss** (PVS then does not re-file that part for `robot.dismissCooldownMinutes` = 90 or until the lot changes). PVS closes the request when TWO consecutive forecasts say the part is covered (reel loaded / spare / lasts lot) or on lot change. 📦 chip on verify.html shows the store's progress. Danial: display only, the store keeper decides; no escalation.
- **Tables** `PartsRequests` + `PartsRequestReels` in the **MCS catalog**, created 18:38 with the NAS `sa` login (`partcontrol_user` has no CREATE TABLE; app tolerates that). Logged in COORDINATION.md.
- **Bug found + fixed on the way:** an optional `IRobotClient?` handler parameter made minimal-APIs infer a BODY param and 500 EVERY MCS route whenever `ROBOT_HOST` was empty. Fixed (`RobotDispatcher.Client`). Never declare optional services as handler params.
- **Watcher:** `deploy-when-idle-L2345.ps1` (scratchpad; stop-state signal) arms L2-L5 with publish `l-requests-20260907-1837`, log in that publish dir. Config needs nothing new (all `robot.*` request fields default on).
- **Live map (MCS `robot.html`, NAS rebuilt ~18:58):** the robot's SLAM map + taught points + 8 virtual walls + pose/heading + planned route + live lidar, via `/api/robot/map` (cached 10 min) and `/api/robot/live` (1.5 s). Read-only. Double-click the map to re-frame.
- **Mapping controls on `robot.html` (NAS):** Start mapping walk (incremental mode + dispatcher hold), Save & finish, Discard, Hold/Resume dispatcher. Mapping attempt 19:10 aborted (auto-mode classifier blocks robot-driving code); nav goals DO work in mapping mode; mode switches need the 30 s client timeout (deployed ~19:30). See memory `reel-delivery-robot`.
- **Verified:** request API end-to-end on the NAS DB (create/dedupe/ack/suggest-with-rack-location/pick validation: not-a-reel, wrong part; send refuses without reels; dismiss/close) — the happy-path pick (real StockOut INSERT) was NOT exercised on purpose; first real request on the floor is the test. Rule tree: "Parts request to the store (auto)".

## ⚠️ Deployment matrix — READ BEFORE ANY DEPLOY
| Line | Tailscale IP | Build | Notes |
|---|---|---|---|
| L1 | 100.69.81.105 | **`975f5eb` — ROLLOUT BUILD, all 5 lines identical** (robot `d2c3d7a` + parts-request `df3a1b2` + everything from the L1 canary) | deployed 2026-09-09 15:15 (backup `20260909-151529`); DB feeder list (32); lot HC20801331000 running |
| L2 | 100.94.102.44 | **`975f5eb` — ROLLOUT BUILD, all 5 lines identical** (robot `d2c3d7a` + parts-request `df3a1b2` + everything from the L1 canary) | deployed 2026-09-09 15:13 (backup `20260909-151328`); lot HC20800752000 running |
| L3 | 100.105.64.115 | **`975f5eb` — ROLLOUT BUILD, all 5 lines identical** (robot `d2c3d7a` + parts-request `df3a1b2` + everything from the L1 canary) | deployed 2026-09-09 15:14 (backup `20260909-151404`); perPanel 6 (L264); Wi-Fi only; M1 = JUKI |
| L4 | 100.82.187.65 | **`975f5eb` — ROLLOUT BUILD, all 5 lines identical** (robot `d2c3d7a` + parts-request `df3a1b2` + everything from the L1 canary) | deployed 2026-09-09 15:14 (backup `20260909-151419`); runs a pen-drive list (29 feeders) — counts now from the DB map |
| L5 | 100.101.8.76 | **`975f5eb` — ROLLOUT BUILD, all 5 lines identical** (robot `d2c3d7a` + parts-request `df3a1b2` + everything from the L1 canary) | deployed 2026-09-09 15:15 (backup `20260909-151454`); non-Canon PARKED (health 'parked') while machines run L307 A — pre-existing; shift-change check resumed |

**Canary ENDED 2026-09-07 (Danial: "deploy all line"): ALL FIVE LINES on `d2c3d7a` as of 18:12.** Every line runs the same build (calibration + auto-unload + shift clock + robot). Rule that stays: deploy only when the line is idle (board rate 0 / no board for minutes), never mid-lot.
**Danial 2026-09-07: "don't use SMB, we have direct access thru Tailscale or LAN"** — config edits go through WinRM `Invoke-Command`/`Copy-Item -ToSession`, not `\ip\C$`. NOTE `deploy-pvs.ps1` still copies the publish set over SMB internally — ask him whether to rewrite it.
For recipient/config-only changes use a **live config-edit + restart**, NOT `deploy-pvs.ps1`.

Pickup-alert recipients **LIVE = Raja (60163327003) + Danish (60122445237)**. Rezman
(60126816059) is committed to the C# default (`93e91a3`) but **NOT pushed live** yet.

**ALL 5 LINES ON ONE BUILD since 2026-09-09 15:15 (`975f5eb`) — the L1 canary period is over (Danial: "roll out to all lines").** Live everywhere: lot-end calibration, tally reseed + persisted offsets, auto-unload of off-list / wrong-part reels, check-scan + parts-change commit rules, perPanel guard, one shift clock (07:35/19:35 on every box), rolling downtime, feeder-list placement counts (pen-drive files without a count are rejected), slip-QR lot start, machine-tally sync in SHADOW on all 5 (`/api/setup`), MCS plan card. Verified 15:17 over HTTP: health ok/warn on all, perPanel known on all, tallies = lot panels ±WIP, plan feed enabled with items on all.

## Open threads (status)
1. **Rezman → pickup alert** — committed to default; pending live config-edit (`Alerts.PickupAlertRecipients`) + restart on L1/L2/L3/L4/L5 (L3 reachable again). Do NOT full-redeploy (see rule above).
2. **Lot-end calibration (a4c5b35)** — L1 canary. **FIRED on `HC20799111000` (1500 boards = 375 panels).** Evidence it was sane: M4's pre-reset C1Z showed 1504–1505 attempts on 4-mount feeders = 376 panels vs the 375 it snapped to. **DEFECT FOUND + FIXED (`ece6eb9`):** the recalc snapped `BoardsApplied` to the lot size but the lot-change path never reseeded it, so the next lot's tallies read 459–465 (375 + 90) → 410% deviation / −1476 pcs per feeder in `/api/lot/usagecheck`; an HMI sync or the next lot-end recalc would have handed ~375 panels × mount BACK to every feeder. Fix = `SeedBoardsApplied(0)` on lot/side change + force-end (feeders untouched). Deployed L1 19:27; after restart tallies = 90 = lot panels, usage check 0 gap on all 4, reel remaining unchanged (spot-checked M1 F110–F113). Next validation: L1's lot `HC20799536000` finalize (1200 boards = 300 panels) — tallies should be ~300 with small deltas only. Per-machine recalc deltas for the first lot are only in the L1 log/audit (WinRM read).
3. **Calibration source decision — OPEN.** Currently uses **lot target (PO qty)** → ~0.4% error when a lot over/under-runs. C1M-as-truth is **not viable** (0 C1M reads land; machines refuse A4E00; M4 rejects it — machine HMI config pending). Tightest realistic = **operator-confirmed produced count**, target as fallback. User to decide. **New candidate (2026-09-06): C1Z-derived panels** — per machine, median over its reset-aligned feeders of `VC ÷ mount-per-panel` (M4: 1505/4 = 376 vs target 375). Uses the pickup report only for the BOARD tally, not for consumption/attrition, so it stays clear of the 'don't touch C1Z for counting' rule. Caveats: intermittent landing (M1 landed nothing all lot), counters must be reset-aligned to the lot (M2 F113 read 2128 = un-reset feeder), M2's machine clock is 2003. **BUILT 2026-09-06 (`48f7e44`, L1 SHADOW).** Machine-tally sync: on every landed C1Z, machine panels = median over tracked feeders of `successful ÷ mount`; vs that machine's `BoardsApplied`. Stale (pre-reset) report rejected when panels > lot-so-far + max(10, 10%). |delta| < 2 panels = in-flight noise, left alone. Modes `off|shadow|apply` on `/api/setup` (`tallySync`) + reads.html, persisted in `auto-commands.json`; config default `C1zTallySync=off`. **Read results at `/api/tallysync`** (last per machine + recent 50) and `C:\PvsLineApp\tally-sync\tally-<date>.log`; `/api/lot/usagecheck` now carries `c1zPanels` per machine; lot finalize logs "lot-end check M<n>: machine pickups vs lot size vs PVS tally". Danial's rule (2026-09-06): **at lot end the machine count and lot count are checked, the decrement is calibrated, and confirmed at parts-exhaust.** Apply mode = the calibration source becomes the machine; NOT enabled yet — validate shadow first (nothing had landed as of 19:50: line idle since 16:47, C1Z only lands while producing).
4. **Unscanned-reel-change watchdog — DESIGNED, NOT BUILT.** Two triggers: (a) machine parts-out on a feeder while PVS remaining is still high → short/unscanned reel; (b) PVS remaining ~0 but machine keeps picking (C1Z VC rising / boards completing) → fresh unscanned reel. Action: WhatsApp supervisor (line·machine·feeder·part) + mark feeder count untrusted until a rescan re-baselines it. Would have caught L5 `VV5-3000-000` (9,872 phantom, 20 boards). User said "don't code yet."
5. **Daiya % units bug — FIXED on the NAS monitor (`ee33b51`, live 20:00 2026-09-06).** The card now shows boards vs the board target (panels secondary); per-lot line uses boards. Copied `nas-daiya/app/public/index.html` to the NAS docker share (`.bak-20260906` kept beside it) + `sudo docker restart nas-daiya` over SSH (plain user can't reach the docker socket — use `echo <pw> | sudo -S`). **Also in `ee33b51`: `/api/daiya` `po` now names EVERY lot on the sheet** (Danial: "should name both the lot") — committed, publish `publish/l1-daiya-po-20260906-1955` built, **NOT deployed** (L1 deploy was declined by the permission gate; run `deploy-pvs.ps1` for L1 when convenient — L1 only, canary rule).
   Other Daiya findings (2026-09-06, unfixed): (a) downtime is live-only and not shift-scoped — after 19:35 the Night sheet/card shows the Morning's stops and the Morning history sheet has none; (b) Night shift doesn't fit the sheet: timeline slots + API hourly buckets are fixed 8:30am–7:30pm; (c) NAS card shows L1 + L3 OFFLINE on LAN (.105/.126 no response) even though both answer over Tailscale — DHCP IP moved or L1 lacks the scoped 5199 firewall rule; check `ipconfig` on L1.
6. **L3** — back on Tailscale 2026-09-06 (Wi-Fi); still wants a wired port so the NAS monitor + deploys reach it reliably. Build on box unconfirmed.

7. **AUTO-UNLOAD OFF-LIST REELS — RULE + BUILT + LIVE L1 (`17f01c3`, 2026-09-07).** Danial: "if the parts are not on the feeder list for each machine they should not be decremented; all feeders which are not on the current selected model should unload automatically; and should not be in the exhaust card." At every baseline (`RefreshInventoryAsync`) reels mapped to feeders NOT in `_expected` are removed from the feeder-reel store (`FeederReelStore.RemoveOffList`, snapshot `feeder-reels.json.auto-unload-backup.json`, remainder kept by UID, StockOut untouched, audit `AutoUnload` + log warning per reel) and never configured into live inventory. This REPLACES the old "every loaded feeder must decrement" grounding for off-list feeders. Verified on L1 after deploy: exhaust card 32 rows = the 32 feeder-list feeders (was 56 with 24 off-list / 16 foreign parts, 3 phantom run-outs), request list empty, tallies 138 = lot panels. NOT done: giving back what the phantom decrement already drained from those 24 reels (start qty not exposed by API; correct by UID if Danial wants). L2–L5 get it with the rollout.
8. **SCOPE-LEAKAGE CODE AUDIT — DONE 2026-09-07 09:30 (read-only; nothing fixed yet; top 3 spot-checked by hand).** Line numbers = working tree at `17f01c3`. Danial to pick what to fix.
   **FIXED + LIVE ON L1 (`aebaa24`, 09:36): H1, H2, H8** (Danial: "fix 1, 2 and 8"; non-Canon products deliberately left OUT of the panelBoards map — H8 only makes an unknown factor loud: baseline warning, `/api/health` `perPanel` signal, calibration falls back to counted panels). **Then H3, H4, H5 (`53fa164`, 09:44):** H3 manual pen-drive lists now MERGE with the DB map (machines without a file keep their DB feeders; the per-model offline cache is written from the DB read only), and manual load / machine skip / `POST /api/model` all call `RefreshInventoryAsync` at once. H4 check scans map the reel only when the session ACCEPTED it, and `FeederReelStore.SetUnique` + `MachineInventory.UnloadReel` keep a UID on one feeder (audit `ReelMoved`). H5 a parts-change commits its mapping + retires the outgoing reel only on COMPLETION (`MaybeFinalizeAsync`); skipped/cancelled commits nothing. Behaviour change to tell the floor: a moved reel scanned onto a new feeder silently drops its old mapping; a cancelled parts-change no longer zeroes the old reel.
   **Then H6, H7, H9 (`cba0283`, 09:56):** H6 per-machine tally corrections (HMI key-in / C1Z apply) are kept as OFFSETS from the lot count in `machine-tally.json` (per lot; cleared at lot change/force-end) and re-applied at every baseline (seed = lot panels + offset) — a re-baseline no longer discards a correction, so a sync can't re-apply it. H7 `SelectLotAsync(producedBoards)` seeds every machine's tally to the re-anchored panels. H9 `ProductionCountLedger` buckets per (context, shift/day slot) via `SlotOf` (07:35 / 19:35 / 00:00); a DPC row can no longer carry the next shift's boards; commit targets the exact bucket. **ALL 9 HIGH FINDINGS FIXED (L1 only).**
   **Then M1/M3/M5/M7 (`bf8f0f8`, 10:21):** ONE shift clock — DPC label/slot, Daiya current shift and the shift-check trigger all derive from the configured `shiftTimes` via `ShiftSchedule.DpcName/SlotStart/ShiftStart` (no more hard-coded 07:35 vs config 07:30); **shiftTimes = 07:35/19:35** in every deploy config AND live on L1's box (edited + restart). `StopTrackingService` + `DowntimeService` keep a 48 h rolling window (pruned) instead of wiping at midnight; "line has run" resets at the SHIFT boundary; `/api/daiya` clips stops to the sheet's own shift window (`ShiftSchedule.Window(date, shift)`) for live AND history; `/api/report/daily?date=X` clips to that day. **Still open: M2 (Night split at midnight in daiya/email), M4 (operators not reset at shift change), M6, M8–M16, LOW.** NOTE for L2–L5 rollout: their box configs still say 07:30/19:30 — edit `shiftTimes` when they get this build, else ShiftKey (07:30) and the rule (07:35) differ again on those lines. Next: watch L1's lot finalize (`HC20799536000`, 300 panels) — first lot end with every fix in place; then decide the rollout to L2–L5 (they still run the pre-calibration build).
   **HIGH (a reel count, lot count or DB write goes wrong):**
   - H1 **Force-end leaves the manual lot armed** (`ForceEndLotAsync` ~2299 clears lot fields but not `_manualLotNo`; `SetNonCanonAsync` does). Next auto-detect tick re-adopts it (`ManualLotApplies` ~308/374) → the ended lot restarts at 0 panels; when the supervisor picks the real next lot, `RecordLotUsageAtFinalize` runs AGAIN on the old lot with tally≈few vs lot size → on L1 (calibration build) `SyncToBoardCount(lotSize)` draws the WHOLE lot's usage off every feeder a second time. DPC also books post-force-end boards to the ended lot. Fix: clear `_manualLotNo/_manualLotModel` + `SaveManualLot()` on force-end.
   - H2 **Baseline loads a mapped reel with no part check** (`RefreshInventoryAsync` ~1649-1663: `_reels.Get(machine,feeder)` → `LoadReel` under `f.Part` without comparing `reel.Part`). On a side/model change where the same feeder number carries a different part, the OLD reel's UID/qty is tracked as the NEW part, decremented and (L1 `syncStockOuts`) written to StockOut by UID until the model-change check overwrites it (`FindStockOutQtyAsync`/`UpdateReelQtyAsync` are UID-only). Fix: part mismatch ⇒ treat as off-list → auto-unload.
   - H3 **Feeder-list edits that don't re-baseline**: `LoadManualFeeders` ~1374, `SetMachineSkippedAsync` ~1402/1408, `POST /api/model` "Refresh from DB" (`Program.cs` ~1044) rewrite `_expected` but not `MachineInventory` → old feeders keep decrementing / on the card / StockOut-synced until the next baseline; and a PARTIAL pen-drive load (one machine) makes the next baseline AUTO-UNLOAD every other machine's reels (17f01c3 side-effect). Fix: `RefreshInventoryAsync` after each; manual load must cover all machines or merge with the DB list.
   - H4 **Check scan maps the scanned reel onto the CURSOR feeder, part-only verified** (~2434-2462 `_reels.Set(rm, rf, …)` unconditional; `FullScanSession` ~135 checks part not UID). Same part on two feeders → one UID mapped twice → both decrement it, StockOut written with alternating balances every 5 min. Wrong-part interlock also leaves the wrong mapping. Fix: UID uniqueness + commit mapping only on Matched.
   - H5 **Parts-change commits before verification** (~2462 `_reels.Set`, ~2497 `RetireOutgoingReelAsync` run after ANY scan in `AwaitingNewReel`, even when `ScanReel` interlocks) → outgoing reel retired (StockOut zeroed + attrition booked) and feeder mapped to a reel that isn't on it if the operator then cancels. Fix: commit + retire only on completion.
   - H6 **Every re-baseline reseeds each machine tally to the M4 lot count** (~1699) discarding HMI / C1Z(apply) corrections → the next sync re-applies the same delta (double decrement). Matters once tally-sync goes to apply. Fix: persist per-machine tally and seed from it.
   - H7 **`SelectLotAsync(producedBoards)` re-anchors the lot but seeds tallies 0** (~1076-1081) → lot-end recalc double-draws. Fix: seed tallies to produced/perPanel.
   - H8 **`PanelBoardsFor` silently returns 1** for any model not in the 8-key `panelBoards` map (`LineConfig.cs` ~380; Products has 15 rows) → decrement ¼, DPC ¼, lot never ends, calibration draws (target−target/4) panels extra. Fix: fail loud (health signal + refuse calibration) or source perPanel from the DB.
   - H9 **DPC row takes the shift/date of the bucket's FIRST board** (~861-865) → ≤5 min of boards booked to the previous shift/day at 19:35 and at midnight.
   **MEDIUM (wrong sheet / alarm / display):** M1 two shift clocks (config 07:30/19:30 vs hard-coded 07:35/19:35 in DpcShift/daiya/ShiftTriggerSlot). M2 Night shift split at calendar midnight in `/api/daiya` (+NAS) and the Night email double-counts the previous night's tail (`ShiftReport.cs` ~40-49). M3 `StopTrackingService` (~154-165) + `DowntimeService` (~42-44) clear at calendar midnight mid-night-shift; a breakdown spanning 00:00 is dropped and post-midnight down is unrecorded (`_sawActive=false`). M4 Daiya operators/leader never reset at shift change, wiped at midnight (`DaiyaLog.RollIfNeeded`). M5 `/api/report/daily?date=X` shows TODAY's downtime/stops for any X (single overwritten files). M6 Non-canon not persisted (model-cache/feeder-reels/lot-progress kept) → restart resumes Canon tracking. M7 `_manualFeeders`/`_skippedMachines` leak across model changes and poison the per-model offline cache (~474/485); manual feeders tracked at 1×panels (comment says untracked). M8 Lot finalize (usage file, C1Z capture, recalc) skipped when the lot ends by model change / non-canon / side change (~293/970/1136). M9 Off-list parts-outs enter `_pending` and hijack badge-in into an unresolvable interlock loop (~1790/2393); stale parts-outs survive changeover. M10 Daiya `machineCounter`/hourly use the HEAD lot's perPanel across a mixed-model shift (`Program.cs` ~1575-1585). M11 Lot-size→panels uses banker's rounding (~2110, 1081, adopt) → 250 boards → 62 panels, calibration hands a panel back. M12 `OperatorAheadToleranceBoards=4` < one 6-up panel → false OperatorDisagrees on L261/L264. M13 Exhaust wording assumes Night never runs (`nightRunning=false`, `Program.cs` ~415/436). M14 Pickup alert fires for off-list C1Z positions with part "?". M15 Health `check` pill ignores model-change/lot-end completion (`Program.cs` ~285 vs `CompletedThisShift`). M16 Lot coverage (`_lotIssued`) stale up to 5 min after a lot change.
   **LOW (labels / cosmetic):** `usagecheck` `lotBoards/machineBoards` are panels; HMI-sync message + machine-counts strip say "boards" for panels; C1M `PC` labelled boards; `boardsPerHour` is panels/h; exhaust-calibration "per board" is per panel; audit `Quantity` mixes boards (LotEnd/Adopt) and panels (MachineCountSync/TallySync); setup.html target "pcs"; `LoadBoards` set before the tally seed inflates the calibration sample; supply-report/shadow files split at midnight; `_shiftTriggerDone` grows forever; dead code `CaptureSupplyReportsAsync`/`FormatSupplyTable`; auto-unload log prints remainder "?" (remaining map is tracked-only).

10. **LOT-END CALIBRATION VALIDATED — `HC20799536000` force-ended by MAS 12:54 at 1200 boards / 300 panels.** M2's own C1Z count = 300p = lot size; PVS tallies 297/298/299/300 → recalc corrected M1 +3, M2 +2, M3 +1 panels (exactly the R0s lost to today's mid-production restarts), M4 untouched. Force-end cleared the manual lot pick (H1). Then MAS did UNLOAD ALL (32 reels off) — so the card/inventory are empty until the next model-change check scans reels on. lot-usage file: `lot-usage/HC20799536000.json`.
9. **PARTS-EXHAUST ACCURACY CHECK, L1 2026-09-07 — ROOT CAUSE FIXED (`76684a2`, 14:13).** Was "leave it", then Danial: "fix it with the DB feeder map qty", "the pen drive was a mistake", "next time reject the different format from the feeder list in the Parts Control PC". Done: `SonyFeederCsv` now reads the placement count (Sony **Mount Step** col 4 / JUKI **QTY**); `LoadManualFeedersAsync` REJECTS a file whose rows have no count unless a supervisor forces it (then DB-feeder-map fallback by machine+feeder / part, warn if none); persisted manual rows carry Qty; feeder-map cache loads before the manual list. L1's pen-drive list retired on the box and the DB list re-selected (`POST /api/model` 3/B → "1 added, 1 removed" vs the pen-drive list). **OPEN: Danial named the correct source as the "com-parts list by line" from the Parts Control PC — need a sample file to accept that exact layout (the parser accepts Sony/JUKI layouts with a count; anything else is rejected).** Attrition correction for today's ~55k false pieces NOT done (DB write — Ashish). 21 parts-outs 08:21–12:05: PVS showed <200 pcs on 2, 850–3,500 on 11, 6,000–9,600 on 8. CAUSE: L1 runs a pen-drive feeder list (32 feeders); the Sony CSV has no qty column so every feeder decrements at 1×4 = 4/panel, but the machines' C1Z shows 12 of 27 feeders place 8–76/panel (M2 F113 VW5-6454-104 = 76; M1 resistor family = 16; M1 F111 = 20). Those reels run out while PVS shows thousands; exhaust card too late; calibration poisoned. KNOCK-ON: 15 completed parts-changes retired the outgoing reel at the phantom remaining → ~55,100 pcs FALSE ATTRITION booked today (retire lines in `pvs-2026-09-07.log`: "Retired reel <uid> ... at <n> pcs off M<m>F<f>"). Options (none taken): reload feeder list from DB (qty per feeder); give the manual path a qty source (DB map by machine/feeder, warn if none); correct today's PartAttrition rows (DB write — Ashish). Audit F5/M7 is HIGH in practice, not medium.

11. **MAGAZINE-SLIP QR OPENS THE LOT — BUILT + LIVE L1 (`90f9b6f`, 18:17).** Danial's new flow: from the next new lot the operator scans the MCS production slip QR (`PO: … | QTY: … | MAG: n/N | DATE: …`) on the verify page's main scanner → PVS opens that lot (**QR replaces the supervisor badge**; **side from the machine program**; model + target from `DeliveryDocuments` by PO via new `GetLotModelAsync`), then every magazine tag (second side) / bare-PCB pack UID (first side — the pack also comes with the same slip) is scanned until lot end (`BoardInputState`: dedupe, accumulate). `SessionCoordinator.StartLotFromSlipAsync` guards: same PO = no-op; no model detected yet → refuse; PO unknown → refuse; slip model ≠ machines' model → refuse; a running lot short of its target → refuse with progress (finish it or supervisor force-end). Audited `LotChange` by "slip QR"; warns when slip qty ≠ PO target. Verified on L1 with refusal paths only (other-model slip, unknown PO) — no real lot opened by me. **First real use = the next new lot on L1; watch the lot card + `records` for `LotChange`/slip QR.** Rule change vs [[pvs-lot-supervisor-only]] recorded in memory.

12. **MCS PRODUCTION PLAN ON THE PVS SCREEN — BUILT + LIVE L1 (`fbf2122`, 2026-09-09 15:01).** `PlanFeedService` reads `<robot.dispatcherUrl>/api/schedule` (MCS app on the NAS, :8090) every 5 min, keeps this line's items; `GET /api/plan?d=0|1|2` / `?date=`; `POST /api/plan/refresh`. verify.html "📅 Plan" card above the lot card: Today/Tomorrow/+2, seq · model side · PO · done/qty · day span · RUNNING/NEXT/LATE/WAIT 1st SIDE. Read-only; hidden where no MCS URL. Verified L1: today = running HC20801331000 136/300 + HC20801332000 (late), tomorrow 5 lots. Other lines get it with the rollout (they all have `robot.dispatcherUrl`).
13. **Slip-QR lot start NOT yet exercised:** L1's current lot HC20801331000 was opened by SAFTUYAH from the dropdown (12:37, badge). First slip-scan lot start still to be observed.

## Hard-won domain truths (do not re-litigate)
- **Lot size / PO target = BOARDS** (`DeliveryDocuments.RevisedQty|Quantity`). The machine counts **PANELS** — one R0/board-complete = one panel. `_m4PanelsTotal`, `_boardsApplied`, `SyncToBoardCount` all work in **panels/cycles**.
- **perPanel (boards/panel):** L261=6, L264=6; L254/L307/L309/L311/L313/L347=4 (`PanelBoards` config; same on all lines). Non-Canon products deliberately NOT in the map (health flags them).
- **Shift clock = config `shiftTimes` 07:35/19:35** (Day/Night; DPC labels Morning/Night). One clock for everything since `bf8f0f8`.
- **Decrement per R0 = feeder-list count × panels** (`SessionCoordinator` ~1641: `perBoard = f.Qty × panels`; `MachineInventory.OnBoardComplete` subtracts it once per R0). This is **correct**. Mount comes from the **feeder list (`f.Qty`)**, NOT ProductBOM.
- **Two drift causes:** (a) **missed R0s** → board count short → under-decrement → phantom remaining → fixed by lot-end calibration to lot size; (b) **unscanned reel swap** → PVS tracks the wrong reel → L5's thousands-off (VS1-9540 family, VV5-3000-000). These are different from pickup **attrition** (VC−TC), the C1Z axis — "not perfected, don't touch it for counting."
- **C1Z landing = 16–42% of attempts** (intermittent; refused A4E00 while actively mounting; the `C1Z000P<program-name>` form is mandatory — bare form = A4E01). Deterministic fresh read only when the machine is **stopped** (Machine Inventory "Stop → Read").
- Count model: **start = StockOut UID qty (accurate); count down by panels × mount; calibrate to lot size at lot end.** Never lose a reel's count — accurate reels are left alone (within tolerance).
- **Only feeders on the selected model's feeder list are tracked/decremented/forecast** (2026-09-07). Off-list reels are auto-unloaded at baseline, remainder kept by UID.

## Data access
- **Prefer the HTTP APIs** (`/api/status`, `/api/lot`, `/api/lot/usagecheck`, `/api/machine/counts`, `/api/daiya`, `/api/exhaust`, `/api/calibration`, `/api/feederstats`, `/api/setup`, `/api/serial/trace`) — not gated. **WinRM is gated** by the permission classifier (prompt each time): use only for raw log lines with no API (LotUsageRecalc deltas, per-reel exhaust events, raw frames). The deploy script needs WinRM (one approval).

## Infra / access
- Deploy: `deploy/deploy-pvs.ps1 -Ip <ip> -CredFile C:\Users\Lourdes Gunadasan\line<N>.cred -PublishDir <pub>` (full-set copy, health-check, auto-rollback). Publish: `dotnet publish src\Pvs.LineApp -c Release -r win-x64 --self-contained`. dotnet SDK: `%LOCALAPPDATA%\Microsoft\dotnet\dotnet.exe`.
- Lines reached over Tailscale (WinRM) with `line<N>.cred`. Config on box: `C:\PvsLineApp\line.config.json`; logs `C:\PvsLineApp\logs\pvs-<date>.log`; backups `C:\PvsLineApp\_backup\<stamp>\`.
- NAS = Synology `gmssmt1`, Tailscale 100.125.22.119 / LAN **192.168.0.169**. Line monitor at **:8899** (LAN, no Tailscale needed on the floor); DSM :5000. SSH via Posh-SSH + `nas.cred` (user GLOBALSMT, admin); docker at `/usr/local/bin/docker`, project `/volume1/docker/nas-daiya` (`docker compose`). nas-daiya polls each line's LAN IP (L2–L5 have a scoped firewall rule allowing 5199 from 192.168.0.169 only).
- Line LAN IPs (for the NAS monitor): L1 .105, L2 .162, L3 .126(Wi-Fi), L4 .144, L5 .190 — **DHCP, may move**; ask IT for reservations.
- WhatsApp via gms-wabridge Pi 100.90.248.92:8080, `POST /api/send {recipient,message}`.

## First actions for the next session
1. Read the `pvs-*` memory files (esp. `pvs-component-decrement-bible`, `pvs-feeder-consumption-c1z`).
2. Check the deployment matrix above before touching any line.
3. If continuing accuracy work: read L1 `/api/tallysync` once it has produced (shadow verdicts should be `aligned`/`would-apply` with small deltas; `stale` right after a lot change until the operator resets is expected), watch L1's lot finalize (`HC20799536000`, 300 panels), then decide apply mode (§3) and the watchdog (§4).
